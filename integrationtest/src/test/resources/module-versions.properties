#This is to let Maven properties be injected in the file, making it easier to run tests in the IDE as well
#module-slot.org.hibernate.ogm.short-id = ${module-slot.org.hibernate.ogm.short-id} #This one requires specific handling, see org.hibernate.ogm.test.integration.testcase.util.ModulesHelper.getModuleSlotString()
module-slot.org.hibernate.search.short-id = ${module-slot.org.hibernate.search.short-id}
dependency.version.org.hibernate.ogm = ${project.version}